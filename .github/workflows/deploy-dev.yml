name: Deploy Dev

on:
  push:
    branches:
      - dev
  workflow_dispatch:
    inputs:
      ref:
        description: "Git ref to deploy"
        required: false
        default: ""
      namespace:
        description: "Kubernetes namespace"
        required: false
        default: "marketplace-dev"
      create_namespace:
        description: "Create namespace if missing"
        required: false
        default: true
        type: boolean

permissions:
  contents: read
  packages: write
  security-events: write
  id-token: write

env:
  DEPLOY_REF: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.ref || '' }}
  DEPLOY_NAMESPACE: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.namespace || 'marketplace-dev' }}
  CREATE_NAMESPACE: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.create_namespace == 'true' || true }}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pipeline:
    uses: ./.github/workflows/reusable-cicd.yml
    with:
      environment_name: dev
      namespace: ${{ env.DEPLOY_NAMESPACE }}
      overlay: dev
      deploy: true
      build_images: true
      push_images: true
      sign_images: true
      run_tests: false
      run_frontend: false
      validate_k8s: true
      validate_k8s_all: false
      policy_check: true
      canary_checks: false
      create_namespace: ${{ env.CREATE_NAMESPACE }}
      ref: ${{ env.DEPLOY_REF }}
    secrets: inherit
